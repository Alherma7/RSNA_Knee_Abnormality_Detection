# EDA, labeler, evaluation, notebook execution. Install with:
#   pip install -r requirements.txt
pandas
numpy
scikit-learn
pytest
pydicom
matplotlib
jupyterlab

# CPU-only here - real GPU training happens on Kaggle. Used for hermetic
# unit tests of tensor-shape/masking/model-wiring logic (A2) with
# pretrained=False (no network download in tests).
torch
torchvision
timm

# Add once needed:
#   monai, snorkel
