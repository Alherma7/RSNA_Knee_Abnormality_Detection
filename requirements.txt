# EDA, labeler, evaluation, notebook execution. Install with:
#   pip install -r requirements.txt
pandas
numpy
scikit-learn
pytest
pydicom
matplotlib
jupyterlab

# Add once real image-model training starts (A2/A3), not before:
#   torch, torchvision, monai, timm, snorkel
